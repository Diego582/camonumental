services:
  web:
    image: nginx:alpine
    container_name: camonumental
    restart: always
    volumes:
      - ./html:/usr/share/nginx/html:ro
    networks:
      - web

networks:
  web:
    external: true
